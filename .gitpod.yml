ports:
    - port: 8000
    - port: 3000
tasks:
  - command: >
          pyenv gloval 3.7.4 &&
          pipenv shell
          pipenv install django 3.7.4 &&
          pipenv install djangoorestframework==3.10.3
          